<h1><%= Hospital.model_name.human %>編集</h1>

<%= render 'form', hospital: @hospital %>
<%= button_to t('helpers.link.delete'), @hospital, method: :delete,
              data: {confirm: t('helpers.notice.delete_confirm')},
              class: 'btn btn-outline-secondary' %>

<%= link_to t('helpers.link.back'), hospitals_path %>
