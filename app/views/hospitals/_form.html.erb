<%= form_with(model: hospital, local: true) do |form| %>
  <% if hospital.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% hospital.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field input-group input-group-sm">
    <div class="input-group-prepend">
      <%= form.label :name, class: 'input-group-text' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>
    <div class="input-group-prepend">
      <%= form.label :expense, class: 'input-group-text' %>
      <%= form.number_field :expense, class: 'form-control' %>
    </div>

    <div class="input-group-prepend">
      <%= form.label :transportation, class: 'input-group-text' %>
      <%= form.number_field :transportation, class: 'form-control' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-outline-secondary' %>
  </div>
<% end %>
