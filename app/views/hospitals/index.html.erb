<p id="notice"><%= notice %></p>

<h1>Hospitals</h1>

<table class='table table-bordered table-hover'>
  <thead>
    <tr>
      <th><%= Hospital.human_attribute_name :name %></th>
      <th><%= Hospital.human_attribute_name :expense %></th>
      <th><%= Hospital.human_attribute_name :transportation %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hospitals.each do |hospital| %>
      <tr>
        <td><%= hospital.name %></td>
        <td><%= hospital.expense %></td>
        <td><%= hospital.transportation %></td>
        <td><%= link_to 'Show', hospital %></td>
        <td><%= link_to 'Edit', edit_hospital_path(hospital) %></td>
        <td><%= link_to 'Destroy', hospital, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hospital', new_hospital_path %>
