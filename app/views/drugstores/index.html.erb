<p id="notice"><%= notice %></p>

<h1><%= Drugstore.model_name.human %>一覧</h1>

<table class='table table-bordered table-hover table-striped'>
  <thead>
    <tr>
      <th><%= Drugstore.human_attribute_name :name %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @drugstores.each do |drugstore| %>
      <tr>
        <td><%= drugstore.name %></td>
        <td><%= link_to t('helpers.link.edit'), edit_drugstore_path(drugstore) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('helpers.link.new'), new_drugstore_path %>
